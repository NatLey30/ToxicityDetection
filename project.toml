[project]
name = "toxicity-classifier"
version = "0.1.0"
description = "A DistilBERT toxicity classifier using the Jigsaw dataset."
authors = [
    { name = "Your Name", email = "you@example.com" }
]
requires-python = ">=3.10"

dependencies = [
    "transformers==4.41.0",
    "datasets==2.18.0",
    "scikit-learn==1.4.2",
    "tqdm==4.66.4",
    "numpy==1.26.4",
]

[tool.uv]
# ensure uv understands we want torch installed from pytorch index
index-url = "https://pypi.org/simple"
extra-index-url = ["https://download.pytorch.org/whl/cu121"]
